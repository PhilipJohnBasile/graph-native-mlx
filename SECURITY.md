# Security and privacy

Do not commit `.graph-model/`, runtime databases, worktrees, model caches, raw prompts, hidden tensors, API credentials, or private repository material.

The checked-in release archives are historical public artifacts. Terminal evidence is sanitized before publication. Report accidental disclosures privately to the repository owner rather than opening a public issue.
